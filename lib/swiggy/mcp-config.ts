import 'server-only';

/** MCP Streamable HTTP protocol version Swiggy expects. */
export const SWIGGY_MCP_PROTOCOL_VERSION = '2025-06-18';

/** Cookie storing the MCP session id across tool calls. */
export const SWIGGY_MCP_SESSION_COOKIE = 'swiggy_mcp_session_id';
